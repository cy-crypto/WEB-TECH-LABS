<%- include('partials/header') %>

<section class="page-header">
  <div class="container">
    <h1 class="page-title">All Gaming Cards</h1>
    <p class="page-subtitle">
      Browse our full collection of rare, epic, and legendary cards. Mix different archetypes to create a deck
      that fits your playstyle, from aggressive rush decks to slow control builds.
    </p>
  </div>
</section>

<section class="prizes-section">
  <div class="container">
    <div class="prizes-container">
      <% products.forEach(function(product) { %>
        <div class="prize-card">
          <div class="prize-image">
            <img src="<%= product.image %>" alt="<%= product.name %>" />
          </div>
          <div class="prize-content">
            <h3><%= product.name %></h3>
            <p><%= product.description %></p>
            <p>
              <strong>Rarity:</strong>
              <span class="badge-rarity"><%= product.rarity %></span>
            </p>
            <p class="price-tag">$<%= product.price.toFixed(2) %></p>
            <a href="/products/<%= product.id %>" class="btn-prize">View Details</a>
          </div>
        </div>
      <% }); %>
    </div>

    <div style="margin-top: 40px;">
      <h2 class="section-title">Build The Perfect Deck</h2>
      <p>
        Start with a core theme, such as dragons, cyber mages, or shadow assassins, and then layer in support
        cards that amplify your strengths. Use legendary cards as finishers while common and rare cards help you
        control the board, draw more options, and outplay your opponent.
      </p>
      <p style="margin-top: 10px;">
        As you extend this project, you can turn this static catalog into a dynamic one powered by a database,
        filters, and search so players can quickly find cards by rarity, cost, or keyword abilities.
      </p>
    </div>
  </div>
</section>

<%- include('partials/footer') %>


