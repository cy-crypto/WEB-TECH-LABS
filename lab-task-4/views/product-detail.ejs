<%- include('partials/header') %>

<section class="product-detail">
  <div class="container">
    <div class="product-detail-layout">
      <div class="product-detail-image">
        <img src="<%= product.image %>" alt="<%= product.name %>" />
      </div>
      <div class="product-detail-info">
        <h2><%= product.name %></h2>
        <p>
          <strong>Rarity:</strong>
          <span class="badge-rarity"><%= product.rarity %></span>
        </p>
        <p class="price-tag">$<%= product.price.toFixed(2) %></p>
        <p><%= product.description %></p>
        <p>
          Each card is printed on high‑quality stock with a durable finish so it can survive countless shuffles
          and matches. The design is tuned for competitive formats, making it a great pick if you want a powerful
          finisher or a standout card in your collection binder.
        </p>
        <p>
          In a real application, this page could also show recommended combos, similar cards, and decklists that
          feature this card so new players can immediately understand how to use it effectively.
        </p>
        <div style="margin-top: 20px; display: flex; gap: 10px; flex-wrap: wrap;">
          <button onclick="addToCart('<%= product._id %>')" class="btn-primary" id="addToCartBtn">Add To Cart</button>
          <a href="/checkout" class="btn-prize">Buy Now</a>
        </div>
        <div id="cartMessage" style="margin-top: 10px; display: none; color: #27ae60; font-weight: 500;"></div>
      </div>
    </div>
  </div>
</section>

<%- include('partials/footer') %>


